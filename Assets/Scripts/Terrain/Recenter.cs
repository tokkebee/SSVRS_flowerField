using System.Collections;
using System.Collections.Generic;
using Unity.VisualScripting;
using UnityEngine;

//This script recenters the Mesh Generator to visually be at 0,0,0
public class Recenter : MonoBehaviour {
    // void Start() {
    //     Vector3 meshGenPosition = GameManager.Instance.MeshGenerator.transform.position;
    //     MeshGenerator meshGenScript = GameManager.Instance.MeshGenerator.GetComponent<MeshGenerator>();

    //     meshGenPosition = new Vector3(-(meshGenScript.terrainSize / 2), 0, -(meshGenScript.terrainSize / 2));
    //     GameManager.Instance.UpdateMeshPosition(meshGenPosition);
    // }

}